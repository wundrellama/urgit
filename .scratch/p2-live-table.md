# CI P2 live table — chair `opus`, close-out cold run on ships `~mep` and `~lut`

- Worktree `/var/home/michael/workspace/urbit/urgit-ci-p2-opus`, branch `ci/p2-opus`, base `master` at `46e9616` (re-freeze 4); the P2 build's record stood at `649d9f5` (its cold run on `~dep`/`~put`, 2026-09-16/17) and the close-out (BRIEF-CI-P2-CLOSEOUT, T1–T8) rides above it. This record is the close-out's cold battery (`.scratch/ci-p2/cold.sh`: `boot.sh` → `boot2` → `docker-rootless.sh start` → `store.sh start` + `store.sh ready` (T3) → `ci-p0/battery.sh` → `ci-p1/battery.sh` → `ci-p2/battery.sh` → `ci-p2/shutdown.sh` (T2)) run detached from fresh piers for both galaxies, nothing typed: launched 2026-09-17 22:42:07, stopped by its own verdict at Q18 2026-09-18 01:23:06 (the first Q18 hit a stale daemon record — Deviations, "the P1 ghost, thrice"), resumed by script on the SAME ships (`COLD_FROM=p2 START_AT=q18`) 01:24:09 → 02:39:06, every step ran, the shutdown included. Every observed value below is from this run; the P2 build's evidence is under `.scratch/tmp/before-closeout/`.
- **First ship** `~mep`, HTTP 8358, pier `/var/home/michael/piers/urgit-ci-p2-mep`, booted by `.scratch/ci-p0/boot.sh` as the tmux session `ci-p2-closeout-mep` (200×50; `tmux new-session -d -s ci-p2-closeout-mep "<urbit> -F mep -B <pill> --http-port 8358 -c <pier>"`, no `-p`), the dojo prompt after 5 s, `|new-desk`/`|mount`/`zig build`/`|commit`/`|install` and `+code` by the script (47 s in all); `%urgit-ci` state version 0. **Second ship** `~lut`, HTTP 8359, pier `/var/home/michael/piers/urgit-ci-p2-lut`, session `ci-p2-closeout-lut`, the same way (`SHIP_ROLE=2`, 46 s), `%urgit` installed; the two galaxies reach each other over Ames (Q5's fork, push and pull request go through `~lut`'s peer protocol). Neither pier existed before the boot: the operator's `~mep` pier from run 1 was `rm -rf`'d first and `ls` confirmed absent; no `~lut` pier existed. Never a herdr pane: every read and keystroke goes through `.scratch/ci-p0/tty.sh` (`tmux capture-pane -J` / `send-keys -l`).
- Rootless Docker: `dockerd-rootless.sh` with state dir `/run/user/1000/ci-p2-closeout/`, socket `/run/user/1000/ci-p2-closeout/docker.sock`, data root `.scratch/tmp/docker-data` (the P2 build's, reused: a rootless data root is subuid-owned and not the user's to remove, and it already held `catthehacker/ubuntu:act-latest`, so the host's rootful daemon was not asked for anything); `info --format '{{.SecurityOptions}}'` → `[name=seccomp,profile=builtin name=rootless name=cgroupns]`, server 29.7.2.
- **Store fixture:** RustFS 1.0.0 (`rustfs/rustfs:1.0.0`) as the rootless container `urgit-ci-store-mep` on `127.0.0.1:8365`, data `.scratch/tmp/store-data` (fresh; the build's is under `before-closeout/`), one private bucket `ci-bucket` (an unsigned list answers 403), region `local-1`, an access key minted into `$TMP/store.env` (mode 600); T3's `store.sh ready` answered `HTTP 403 (try 1)` in `cold.sh` and again in `p2-setup`; `store.sh configure` ran the P0 `%storage-action` recipe (H2 and p2-setup). Every read of the bucket in the rows is `curl --aws-sigv4` with that key or a bare `curl` of a presigned link.
- Daemon a: enrolled once by `p2-setup` at `DAEMON_CAPACITY=3` (T1; the footer's value, `ci-p1/env.sh`), `0v5.ko4ia.aunoo.7nmrc.rcrvv.emtdd`, never restarted at another capacity; Q18 read `enrolled capacity 3` from its config and ran.
- Tooling: `go1.27.1`, `zig` 0.15.2, `act` 0.2.89 static (`act-static.sh`, kept from the build), ERPit source `/var/home/michael/workspace/urbit/erpit` at `a6d15ed` (read-only, cloned into the fixture repositories), tmux 3.7c.

## Commits (one per stage, plus the ruled amendments and the close-out)

| Stage | Commit | Gate |
|---|---|---|
| S0 | `e2f1225` spec: §Execution gains §Delivery (D7) | spec only, first in the sequence |
| S1 | `5fa71f7` footer env, RustFS fixture, sur, presign-get, log upload, the read routes (D1/D2) | `ci-storage-vector` %.y; the ship-signed PUT lands in RustFS, the presigned GET reads it, unsigned 403, expired 403 |
| S2 | `c9839b6` web-merge gate, trust at staging, restricted policy, approval (D3/D3a) | Q5a–Q8 live; `git-access-vector` %.y |
| S3 | `032c456` credentials, grants, the scrub on both sides (D4) | `ci-event-vector` %.y, `go test` |
| S4 | `bcafb5f` the CI signing key, certified; assignments and grants signed; the daemon verifies (D5) | `sig` tests incl. the dojo jam vectors and the live certificate |
| S5 | `60b2fd9` the ci/* routes, `POST ci/action`, the CI tab and the settings controls (D6) | `npm test` 132/132 |
| S6 | `64a3eee` README, the boxes, the battery scripts; the build's record | the build's cold run (`~dep`/`~put`) |
| S2 amendment (re-freeze 2) | `015fa83` `ci-can-write` decides trust and approval; the second galaxy's pull request drives Q5–Q8 | Q5a, Q5–Q9 live on two ships |
| S2 amendment (re-freeze 3) | `d9e54fc` touch 5 as ruled: the writer helpers lifted out of on-poke's local core | peek %.y/%.n/%.n; Q5, Q8 |
| CI-SANDBOX-1.1 (re-freeze 4) | `3b50b00` `--container-daemon-socket`; Q19 RED on the unfixed daemon, GREEN on the fixed one | Q19 |
| harness (build) | `344cd09` `b5c40a3` `8beb59a` `affb015` `86e3700` `649d9f5` | the fixes the build's RED/GREEN phases and cold runs found |
| close-out T1 | `4e85c02` the enrolled capacity is the footer's `DAEMON_CAPACITY` (3); q18 refuses below 3 | p2-setup enrolls once; Q18 ran on that daemon |
| close-out T2 | `a56e03b` shutdown by /proc-verified PID, never herdr; ships in tmux; the close-out footer | this run's boots and its shutdown |
| close-out T3 | `a6b50f3` p2-setup asserts the store answers before Q2 (`store.sh ready`) | `HTTP 403 (try 1)` twice in this run |
| close-out T4 | `1dfc616` the tripwire table takes the tighter string per row (Q5a, Q6, Q13, Q16 changed) | groups A/B/C RED 11/11 with every tripwire present, 0 wrong-reason |
| close-out T5 | `e735b13` Q19 walks every mount and bind of the live job container | Q19 GREEN 0 of 4 mounts; RED 2 (the host bind in `.Mounts` and `.Binds`) |
| close-out T6 | `df3f473` the record convention: `provider-blocked, not run` | Deviations |
| close-out T7 | this run's harness (`cold.sh` resume path, the Q18 ghost wait) | the battery above |
| close-out T8 | this record | — |

## The five `%urgit` touches (fence, re-freeze 3)

1. `handle-api`, the `pulls/<n>/merge` route: the CI gate, `urgit.hoon:7395–7435` — a CI-protected target is never written; the pull's head is staged against the current tip with `actor=source-ship.pull`, `via=%session` and the pull number; 202 with the candidate id and the class the ship will find.
2. `land-candidate`, `urgit.hoon:8051–8095` — takes `pull=(unit @ud)` and flips that pull to `%merged` inside the landing event (`:8079–8090`).
3. `handle-api`, the `GET /repository/<name>` route: `ciUntrustedPolicy`, `urgit.hoon:6119–6137` — `%urgit-ci`'s `/policy/<repo>` under the CI gate's guards, `null` when unreadable.
4. `handle-receive-pack`, the push caller, `urgit.hoon:8734–8744` — the stage poke passes `pull=~` (and no class: the ship decides).
5. `on-peek` `[%x %ci-can-write @ @ ~]`, `urgit.hoon:9302–9316`, over `repository-writable:hc`; the lift: `=<` at `:2231`, the helper door `|_ =bowl:gall` at `:10343` with `group-peek :10357`, `group-seat :10387`, `repository-group-capability :10416`, `repository-writable :10423` (bodies unchanged), and the same-name wrappers in on-poke's `|^` at `:2322`, `:2327`, `:2350`, `:2367`.

## P0 battery tail (`.scratch/ci-p0/battery.sh`, inside `cold.sh`)

Run 22:43:43–22:48:34 on `~mep`, straight after both boots, the rootless daemon and the store (`store_up` = `store.sh start` + `store.sh ready`: `HTTP 403 (try 1)`). Every step ran; H1 refused CI without storage; H2 pointed `%storage` at the RustFS fixture on 8365; the seven negative rows went RED under the P0 mutants and GREEN on the real build; the four P0 vectors print `%.y` and `cd fe && npm test` passes 132/132.

```
== red: PASS=0 () FAIL=7 (H9 H10 H11 H12 H13 H14 H15) of 7 rows; build: MUTATED
== green: PASS=7 (H9 H10 H11 H12 H13 H14 H15) FAIL=0 () of 7 rows; build: clean (real build)
battery.sh: all steps ran (2026-09-17T22:48:34-05:00)
```

## Q17 — the P1 battery (`.scratch/ci-p1/battery.sh`, 22:48:34 → 00:11:30)

P1–P20 all PASS on this tree; P15 (ERPit, eight jobs, daemon a at capacity 3 from `p15-prep`) in **894 s** push to verdict, all eight `jobResult "success"`; the thirteen P1 mutants RED with their tripwires (23:23:31 → 00:03:22) and GREEN on the real build (→ 00:11:02); the foreground (every `+urgit!*-vector`: nine assert `%.y`, fourteen dump values; `npm test` 132/132; `go test`, `-race`, the live Docker boundary test on `unix:///run/user/1000/ci-p2-closeout/docker.sock`, vet/gofmt, the static binary) 0 failing.

```
P1: PASS P2: PASS P3: PASS P4: PASS P5: PASS P6: PASS P7: PASS P8: PASS P9: PASS P10: PASS
P11: PASS P12: PASS P13: PASS P14: PASS P15: PASS P16: PASS P17: PASS P18: PASS P19: PASS P20: PASS
== red: FAIL-with-tripwire=13 (P1 P7 P8 P9 P10 P11 P12 P13-overlap P17 P18 P19 P20 P14) FAIL-wrong-reason=0 () PASS=0 () of 13 rows; build: MUTATED
== green: PASS=13 (P1 P7 P8 P9 P10 P11 P12 P13-overlap P17 P18 P19 P20 P14) FAIL=0 () of 13 rows; build: clean (real build)
== vectors failing: 0; go steps failing: 0
battery.sh: all steps ran (2026-09-18T00:11:30-05:00)
```

## P2 table (`.scratch/ci-p2/battery.sh`, 00:11:30 → 01:23:06 and 01:24:09 → 02:38:57)

Real `act` 0.2.89 in the docker-rootless sandbox, the real daemon binary, the real store, the second galaxy's real pull requests, no dojo poke between push and verdict except the rows' own operator acts. Rows driven by the battery on repository `ci-p2` (created by `p2-setup` at 00:11); logs in `.scratch/tmp/p2-*.log`. "Observed" quotes the row's PASS lines verbatim (trimmed). Q18 ran twice on this ship: the first attempt (`erpit-q18-002059`, 00:20:59) is **FAIL** — a job handed to a stale daemon record, `%unknown` at the ~h1 deadline (Deviations); the battery stopped there, the harness gained the ghost wait, and the resumed battery's Q18 (`erpit-q18-012411`, 01:24:11) is the row below. Q14–Q16, Q19, the mutant groups and the foreground ran in the resumption, on the same ships, nothing typed.

| Row | Verdict | Observed |
|---|---|---|
| Q1 | PASS | `git log`: `e2f1225 ci-p2: S0 — spec …` is the first commit above the footer, before any code (`5fa71f7` S1 follows); the close-out's T1–T8 ride above `649d9f5`, none touching `desk/` or `runner/` |
| Q2 | PASS | the `pass` job's attempt `0v7.i2l25…` `%passed`; `attempt.log = [~ [key='ci/ci-p2/0v19d74…/0v7.i2l25…/trusted/log.jsonl' size=4.410 sha256='563fed2b…']]`; `GET ci/attempt/<id>/log` → **302** to `http://127.0.0.1:8365/ci-bucket/ci/ci-p2/…/trusted/log.jsonl?X-Amz-Algorithm=AWS4-HMAC-SHA256&…&X-Amz-SignedHeaders=host&X-Amz-Signature=…`; a bare `curl -L` → **200**, body sha256 `563fed2b…` = the ship's handle = the daemon's saved stream, 4410 bytes, a `jobResult` line inside; unsigned GET **403**; signed HEAD 200; a 2 s presign reads 200 now and **403 `Request has expired`** after 4 s (00:13:50) |
| Q3 | PASS | attempt `%trusted`; key carries `/trusted/`; `sign-get` and `presign-get` for `%untrusted` on that attempt → `~`; `%trusted` signs `/trusted/log.jsonl` |
| Q4 | PASS | on a RUNNING attempt (fixture-wait): `POST upload name=../x` → **400** `name must be log.jsonl, summary.md or artifact/<file>`; `artifact/../x`, `artifact/a/b` → 400; `artifact/ok.txt` → 200 signed under `/0v5.0p0ig…/trusted/artifact/ok.txt`; a good name on the closed Q2 attempt → 409; no credentials → 401; the wait job `%passed` |
| Q5a | PASS | a writer's PR (`q5a-feature-001616` landed directly, `POST pulls` #1) to the CI-protected master, Merge → **202** `{"actor":"~mep","candidate":"0v6.kmih6…","trust":"trusted","staged":true}`; master unmoved (`3d989395…`), the pull `open`; candidate `%trusted`, `pull=1`, actor `~mep`; `%passed`; **master = the candidate object** `dd43503f…`, the pull `merged` only then, `'landed'`. A PR to an unprotected branch: Merge → 200, the branch moved to the merge commit `e7e7fb06…`, `merged` at once |
| Q5 | PASS | `~lut` forks `ci-p2` through the peer protocol (`ci-p2-fork-001625`), pushes `q5-contrib-001625`, opens pull request **#3** (`sourceShip ~lut`); the writer merges it through the web → **202** `trust: untrusted, actor: ~lut`, candidate `0vmjksb…`; `%untrusted %pending`, actor `~lut`, `pull=3`, materialized, `plan=~`, **0 attempts**, the daemon's log names it 0 times; master unmoved (`dd43503f…`); the pull `open` |
| Q6 | PASS | `%set-untrusted-policy %restricted` → the same candidate planned and run: 2 attempts, **2 `%untrusted`**; the job's log key under `/untrusted/log.jsonl`; the daemon's assignment line `trust untrusted`; `%passed` with `verdict-reason` containing `untrusted candidate cannot land`; master unmoved (`dd43503f…`); the pull `open`; the eligibility scry `%.n` |
| Q7 | PASS | `POST ci/action {approve-candidate}` (the session) → 200; the old candidate `%skipped` `'superseded by approval'`; the trusted twin (same head `654cabd9…`, `pull=3`) `%passed`; **master = the twin's object** `654cabd9…`; `'landed'`; the pull `merged`; the old one still `%skipped` after the twin's attempts closed |
| Q8 | PASS | pull request **#4** from `~lut` (fork `ci-p2-fork-001746`), staged `%untrusted`; `ci-can-write` `%.n` for `~lut`, `%.y` for `~mep`; `%approve-candidate … ~lut` → **refused `actor cannot write ci-p2`**, still `%untrusted %pending`, no twin; `|rein %urgit [%\| %urgit]` → the owner's approval refused **`ci: %urgit cannot be read; the actor cannot be checked`**; `%urgit` revived (`%gu` `%.y`) → accepted; old `%skipped`, the twin `%trusted`, `%passed`, landed `08852481…`, the pull `merged` |
| Q9 | PASS | `%set-credential TOKEN` (`%job`) listed by `credential-names`; a 3-character value refused `at least 8 characters`; a value with a newline refused **`must be a single line`**; fixture-secret's job `%passed`; the daemon's assignment line **`grants 1 (TOKEN) of 1 offered`**, `--secret TOKEN=***` in its log, the value 0 times; the bucket log: `token is ***` and the value's length, the value 0 times; the saved stream = the bucket object (sha256 `e7a4f654…`); the step's set-output of the secret recorded as **`'***'`** on the ship; the saved stream's set-output line carries no raw copy |
| Q10 | PASS | the stage poke by `~sampel-palnet` (the peek refuses it) under `%restricted`: `%untrusted`, the job `%passed`, attempt `%untrusted`, **`grants 0 () of 0 offered`**, the step saw `token length 0`, the value absent, the credential still stored |
| Q11 | PASS | the attempt's recorded output `'***'`; seven scry dumps `grep -c value` **0**; `GET ci/attempt/<id>` 0; the log route's object 0; no key named by the value; `%delete-credential` → not listed; a `%rerun-candidate`'s assignment `grants 0 () of 0 offered`, its step `token length 0` |
| Q12 | PASS | `GET ci/key` → 200 `{pub, cert, certified: true, ship: ~mep, …, shipSigningKey}`, pub 32 bytes, cert 64, no `sek`/`sec`/`ring`; without a session 401; daemon a pinned the same pub (`bbb0e429…`); **Go's `crypto/ed25519` verifies the certificate** (`TestLiveCertificate`); a pushed job `%passed` with `assignment signature verified` in daemon a's log; daemon b (`0v7.63ate…`) started with `ci_public_key` = the pub's first digit flipped (`fbb0e429…`), the operator `%assign`s the job to it: **`assignment refused: signature does not verify`**, no sandbox prepared, the attempt `%infrastructure-error` with that reason |
| Q13 | PASS | the normal attempt got the grant; daemon a stopped, its dead poll forgotten, `%assign` with a 25 s deadline, a started 15 s later behind a docker wrapper delaying the first `cp` by 12 s, act exited at +30 s; **`grant TOKEN refused: expired at 05:20:47Z (now 05:20:52Z)`**, `grants 0 () of 1 offered`; the stream `token length 0`, no non-zero length |
| Q14 | PASS | `GET ci/repository/erpit-q18-012411/candidates` → 200, newest first, every candidate with ref/head/actor/status/trust/created/attempts; the ERPit candidate lists **8 job attempts**; `GET ci/candidate/<id>`: 8 rows, all `passed`, **8 log handles**, `verdictReason: landed`; the log route's jsonl rendered through `fe/src/ci.js` `renderLog`: 161 lines, `[job] step: msg` events, act's banner kept as text (`Using docker host 'unix:///var/run/docker.sock', and daemon socket 'unix:///run/user/1000/ci-p2-closeout/docker.sock'`), result lines marked |
| Q15 | PASS | `set-ci-protected` on → policy lists the ref, the scry agrees; off → gone; a ref with no tip → **409 `ref has no tip`**; the untrusted radio round-trips `restricted`/`approval`; the credential form adds `Q15_TOKEN env staging`, the credentials/policy/repository reads, the built `desk/web` and the served app carry the value **0** times; a short value → 409; delete → not listed; a refused action → 409 `no such candidate`; an unknown action → 400 |
| Q16 | PASS | every `ci/*` read (candidates, candidate, policy, credentials, key, attempt log) without a session → **401**; with a session → 200 (the log route 302/404); `POST ci/action` without a session → 401, with → 200; a daemon bearer is not a session (401) |
| Q17 | PASS | the P1 battery above: 20/20, mutants 13/13 RED and 13/13 GREEN, foreground 0 failing |
| Q18 | PASS (second run; the first is FAIL — Deviations) | ERPit master `a6d15ed` (1067 commits) seeded into `erpit-q18-012411`, CI-protected; every daemon record but a stale (the ghost wait, 01:24:11); one commit pushed: plan after 46 s = the eight jobs; **8/8 `%passed`**, no reruns, the gated four after the ship read their plan outputs; candidate `%passed` after **1163 s**; **8 log handles, 8 keys `ci/<repo>/<cid>/<aid>/trusted/log.jsonl`, 8 bucket objects whose sha256 match the ship's handles**, none under `/untrusted/`, an unsigned read 403; landed: master = `c391fef4…` |
| Q19 | PASS | fixture-docker's job live: the job container's `Binds=["/run/user/1000/ci-p2-closeout/docker.sock:/var/run/docker.sock"]`, the docker.sock mount's source = the configured rootless socket, the host's rootful socket not bound; **T5: 4 mounts walked** (the socket bind, the `act-toolcache` volume, two act volumes), the host socket absent from every mount and bind (0); `docker info` inside the job → **`name=rootless`**, no permission denied. RED in group A (the flag dropped): `Binds=["/var/run/docker.sock:/var/run/docker.sock"]`, the walk finds the host socket **2** times (`.Mounts` and `.Binds`), `security=[]`, `permission denied` |

### Q18 timing (ERPit, daemon `a` at capacity 3, the resumed run)

Push at 01:24:11 (local); plan attempt 46 s (clone of 1067 commits + `act -l` ×2); the eight jobs assigned to daemon a alone (three at once, the rest as slots freed), `suite` last; **push → verdict 1163 s = 19 min 23 s** (P15 in the same run: 894 s; the build's Q18: 860 s). Eight uploads before eight results, in daemon a's log: `pins` 7708 B, `fixtures/plan` 6856 B, `suite/plan` 6182 B, `structural` 22533 B, `replay` 58281 B, `erasure` 63554 B, `duo` 73501 B, `suite` 50139 B; the ship recorded eight handles whose sha256 the signed reads confirmed.

The first Q18 (`erpit-q18-002059`, push 00:20:59, plan after 40 s): three jobs and the plan on daemon a, `fixtures.yml/plan` handed to the stale record of Q12's stopped daemon b, never fetched, `%infrastructure-error 'no result arrived before the deadline'` at 01:21:30, candidate `%unknown` `'job plan in fixtures.yml had an infrastructure error'` after **3627 s** — `keep-q18-ghost-first*.log/json`; Deviations.

## Mutant pairs (P2 negative rows: Q4, Q5a, Q5, Q6, Q8, Q10, Q11, Q12, Q13, Q16, Q19)

`.scratch/ci-p2/q-mutants.sh apply <rows>` puts the named rows' one-line sabotages into one build (five files: `desk/lib/ci-storage.hoon`, `desk/app/urgit-ci.hoon`, `desk/app/urgit.hoon`, `runner/internal/daemon/daemon.go`, `runner/internal/relay/relay.go`); `q-negatives.sh red <rows>` rebuilds the desk and the daemon, seeds a fresh CI-protected repository (`ci-p2-red-<hhmmss>`), keeps daemon a's identity at `DAEMON_CAPACITY`, runs a prep (Q2's push; Q9's before Q11) and every row, counting a row RED only when it FAILS **and** its log carries `q-mutants.sh tripwire <row>` (T4's table); it reverts the working tree on EXIT; `green` reverts, rebuilds and repeats. Three groups: A = the eight rows plus Q19 (01:45:49 → 02:08:24), B = Q5 (→ 02:23:27), C = Q8 (→ 02:38:30). `desk/` and `runner/` clean after every phase (`git status`: only `.scratch/`).

```
== red: FAIL-with-tripwire=9 (Q4 Q5a Q6 Q10 Q11 Q12 Q13 Q16 Q19) FAIL-wrong-reason=0 () PASS=0 () of 9 rows; build: MUTATED
== green: PASS=9 (Q4 Q5a Q6 Q10 Q11 Q12 Q13 Q16 Q19) FAIL=0 () of 9 rows; build: clean (real build)
== red: FAIL-with-tripwire=1 (Q5) FAIL-wrong-reason=0 () PASS=0 () of 1 rows; build: MUTATED
== green: PASS=1 (Q5) FAIL=0 () of 1 rows; build: clean (real build)
== red: FAIL-with-tripwire=1 (Q8) FAIL-wrong-reason=0 () PASS=0 () of 1 rows; build: MUTATED
== green: PASS=1 (Q8) FAIL=0 () of 1 rows; build: clean (real build)
```

What turned each row red — the mutant and the tripwire (T4's string, as the row's log carried it):

- **Q4** — `upload-name-allowed` passes every name. `upload name=../x -> 400: FAIL (observed: 200` (a signed URL for `…/trusted/../x`).
- **Q5a** — the merge route's gate skipped (`?: %.n`). `Q5a RED: protected master moved before checks` — printed by the row after it read the ref CI-protected (`%.y`) and master moved at the click (the merge answered 200 `{"ok":true,"commit":…}`).
- **Q5** — staging classes every actor `%trusted` whatever `ci-can-write` answered. `candidate is %untrusted: FAIL (observed: %trusted` (the candidate found under the trusted id).
- **Q6** — `++landable` ignores trust. `Q6 RED: untrusted candidate landed` — printed after the candidate read `%untrusted` and master equalled its object (`4a7a138e…`; verdict-reason `'landed'`).
- **Q8** — approval accepts an actor the peek refused. `approval by the second galaxy refused: FAIL (observed: accepted (>=)`.
- **Q10** — grants released to an `%untrusted` job. `grants=~ on the untrusted assignment: FAIL (observed: grants 1 (TOKEN)`.
- **Q11** — the daemon's line scrub skipped. `the log route's object: grep -c value = 0: FAIL (observed: 1` — act's set-output line's raw `arg` reached the bucket (the ship's own scrub kept its state at `***`; the Q9 prep under this build carried the value in the stream and the bucket, the same effect — Deviations).
- **Q12** — the assignment signature not verified. `daemon b prepared no sandbox (no work): FAIL (observed: 1` — the wrong-key daemon ran the job.
- **Q13** — the expiry check skipped. `Q13 RED: expired signed grant reached act` — printed after the late attempt's stream showed a non-zero token length with act past the 25 s expiry (+30 s).
- **Q16** — `++viewer` answers yes to anyone. `Q16 RED: anonymous CI reads and action accepted` — printed after all five reads and `POST ci/action` answered 200 without a session.
- **Q19** — `--container-daemon-socket` dropped. `the job container's socket bind is the rootless socket: FAIL (observed: /var/run/docker.sock:/var/run/docker.sock`; T5's walk: the host socket 2 times.

## Foreground

`.scratch/ci-p2/foreground.sh` = P1's (`.scratch/tmp/p2-foreground.log`, 02:38:30–02:38:57): every `+urgit!*-vector` (nine assert `%.y`, fourteen dump values), `cd fe && npm test` **132/132**, `go test ./...` and `-race` (incl. `internal/sig` and the grant/signature daemon tests), the live Docker boundary test on the harness socket (`TestDockerLiveBoundary` PASS), `go vet` + `gofmt` clean, the static binary (`statically linked`): **vectors failing: 0; go steps failing: 0**.

## Deviations (the build, kept; the close-out follows)

The build's deviations stand as its chair wrote them (its `.scratch/tmp/keep-*` and `p2-*` paths now live under `.scratch/tmp/before-closeout/`). Every derivation was built as written unless listed here. Each departure cites what made it. The three boxes the chair raised (§1 the second ship, §2 multi-line secrets, §3 the writer read) were ratified in re-freeze 2 and built in the S2 amendment commits; re-freeze 3 ruled the shape of touch 5 and re-freeze 4 added CI-SANDBOX-1.1; `QUESTIONS-CI-P2.md` holds no open box.

- **Record convention — a provider safeguard is not a verdict (close-out T6, adopted from astra's record).** A row that a provider safeguard interrupted is recorded as `provider-blocked, not run`, with the timestamp of the interruption, never as PASS or FAIL; it is re-run at most once, and a second interruption leaves it `provider-blocked, not run` and ends the run. The rule is astra's, in its record's words: "Rows left unexecuted in that preliminary phase are recorded as **provider-blocked, not run**" and "One provider retry is authorized; a repeated block ends the run with the remaining row recorded as `provider-blocked, not run`" (`.scratch/astra/p2-live-table.md`, "S6 preliminary battery — interrupted; superseded by cold run"). Applied to this record: every row's verdict is from its own run; a row this rule ever claims is marked in the table as `provider-blocked, not run (<timestamp>)` in the Verdict column, and no row of this record is.

- **Touch 5 — the lift, as ruled.** `on-peek` cannot see on-poke's local `|^` core (the P1 build note), so `group-peek`, `group-seat`, `repository-group-capability` and `repository-writable` moved, bodies unchanged, into a helper door the agent door reaches as `:hc` with its bowl set (`=<` before the door, `|_ =bowl:gall` after it — the agent door's `^- agent:gall` cast is exact and takes no extra arm). One-line wrappers of the same names stay in the `|^` so every existing caller keeps its spelling. The peek `[%x %ci-can-write @ @ ~]` asks `repository-writable:hc`; `%.n` for a repository this ship does not hold or an actor that is not a `@p`. The re-freeze 2 amendment had solved the read another way — a bowl-taking duplicate of the predicate (`ci-can-write`) in the library core, reading the `%groups` seat itself; the re-freeze 3 commit removed it and aligned to the lift. The fe source test that reads the `%groups` readers' bodies now takes their last occurrence (the lifted bodies), not the first (the wrappers).
- **Touch 3 — `ciUntrustedPolicy`.** `repository-json-up-to` sits in the library core, which has no bowl; the field is added at the authenticated `GET /repository/<name>` route in `handle-api`, from `%urgit-ci`'s `/policy/<repo>` peek under the CI gate's guards, `null` when unreadable. The public route does not carry it; the settings page reads it and falls back to `GET ci/repository/<name>/policy`.
- **Touch 4 — the receive-pack caller passes `pull=~`** and nothing else: the trust class is `%urgit-ci`'s finding through the peek for every caller, so `%stage-candidate` carries no class (re-freeze 2 dropped the `trust` field the S2 commit had added). The merge route still names the id the ship will give (it applies the same rule to answer 202), which is why Q5's and Q8's rows look the candidate up by both ids under the mutant.
- **D3 — trust and approval read the peek and refuse on ~.** `++can-write:urgit-ci` reads `/ci-can-write/<repo>/<actor>` through the guarded adapter; an unreadable answer refuses the staging (`'ci: %urgit cannot be read; the actor cannot be classified'`) or the approval (`'… cannot be checked'`, Q8's suspended-`%urgit` probe) rather than assuming a class. In P2 the only actor that can reach the approve poke is the owner (the session); a listed writer on another ship has no session and no cross-ship approve route — the P3 item is a `%git-peer` approve carrying `src.bowl`, which would need no change to the rule.
- **D3 — candidate ids and the shared scratch ref.** The trusted id stays `(sham [repo ref head base])` — the id `%urgit` prints in the push answer and names the scratch ref by — and the untrusted twin adds the class; a rerun adds `now`. Every candidate of one head and base shares `refs/ci/candidate/<sham repo ref head base>` (materialization re-sets what a close released) and the assignment carries `scratch-ref`. `%candidate-ready`/`%candidate-conflict` update every unmaterialized twin. `candidate-status` gains `%skipped`; `settle` leaves a `%skipped` candidate alone whatever its attempts do.
- **D3 — one arm decides landing.** `++landable` (passed, trusted, materialized) is read by both the eligibility scry and `settle`'s landing request, so a restricted check cannot advance a ref from either side and one mutant (Q6) opens both; a passed untrusted candidate's `verdict-reason` names trust and `%urgit`'s `land-candidate` is never asked.
- **The second galaxy (§1, option (a)).** `~put` boots from the footer with `SHIP_ROLE=2` switching every P0 driver (pane id, `+code`, cookie jar); `.scratch/ci-p1/peer.sh` forks, pushes and opens the pull request on it, matching every request by its own id, so Q5–Q8 run on a real non-writer author. A fork checks that the origin's default branch exists: a repository created through the API defaults to `refs/heads/main` while the harness pushes `master`, so the first fork failed `'received repository graph is incomplete'` (found with a throwaway debug print on `~put`'s desk only) and `p2-setup` sets the default branch to master. A contribution writes a file of its branch's own, and Q8 forks afresh, because a second branch from a stale fork conflicted with what Q7 landed.
- **Q8 is three probes.** The owner cannot be removed from its own write set, so the row refuses the second galaxy (`'actor cannot write <repo>'`), refuses the owner while `%urgit` is suspended by `|rein` (the read is unavailable), and admits the owner once `%urgit` is back; the twin runs, lands and flips the pull.
- **D1 — no HEAD at first read.** The ship has no outbound HTTP; a result that names a log records the handle under the attempt's own key, and an object the store does not hold surfaces as the store's answer to the 302. A result that names no log leaves `log=~` and keeps its verdict.
- **D2 — Q2 runs the fixture `pass` job, not ERPit's `plan`.** A plan attempt has no act stream to upload; ERPit's eight job logs are Q18's. Q4 probes the name fence on a running attempt (fixture-wait) so a build without the fence answers 200 with a signed URL — the tripwire; the fence runs before the state check, so a closed attempt answers 409 to a good name only.
- **D4 — credential values are single-line and at least eight characters** (§2 as ruled: the ship refuses a newline; per-line scrubbing is P3's; the set-output `arg` scrub is spec). act's set-output line carries the raw value in `arg` while masking `msg` (measured, `.scratch/tmp/keep-actprobe-secrets.jsonl`), so both the daemon's line scrub and the ship's `scrub:ci-event` exist; Q11's mutant is the daemon's scrub, because under one build the ship's scrub cannot be turned red while the daemon still scrubs (that layer is the event vector's). Q10 stores its own credential.
- **D4 — a grant expires at the attempt's deadline** (`assigned + deadline`), measured from the ship's assignment time; the daemon's own deadline runs from receipt, which is what Q13 uses (a late delivery through a dead poll forgotten first). The `dbug` wrapper's state scry would print credential values: a developer surface, not a product scry or route; every product read greps the value 0 times (Q11, Q15).
- **D5 — the key exists from `on-init`, not from the first `%set-credential`.** "The daemon refuses an unsigned assignment" cannot hold if the key does not exist at the first enrollment; Q17's P1 regression carries no credentials and must still verify. `%rotate-ci-key` keeps the operator's rotation.
- **D5 — the ring is not kept; the derived signing pair is.** Jael's `%private-keys` gift yields the current ring; a suite-b ring is `'B'` then 64 bytes with the signing seed in the low 256 bits (`nol:nu:crub`, zuse:1806 upstream), and `(luck:ed:crypto seed)` is the `[sgn.pub sgn.sek]` ames signs with. `ship-keys` holds `[life pub sek]` of that pair only — a strict subset of the ring, never logged or read out. Go's `crypto/ed25519` verifies the certificate live (`TestLiveCertificate` on `GET ci/key`, Q12).
- **D5 — the signed noun and its expiry.** `(jam [recipient attempt operation expiry nonce])` with `expiry` in unix seconds (a `@da` is not a JSON number) and `operation` `'assign'` or `'grant:<name>'`; the daemon rebuilds the noun from the wire fields and reimplements `++jam` (vectors read off the dojo, including the backreference case). An assignment's authorization expires a minute past the attempt's deadline (delivery is not instant); a refused assignment is abandoned with the reason so the ship shows why, rather than dropped silently. `GET ci/key` is session-authorized like every ci/* read.
- **D6 — times on the wire are unix seconds**, and `POST ci/action` applies the poke under `mule` so a refusal answers 409 with the `~|` message's own words (the first quoted cord in the trace; a `rap`-built reason is cast `@t`, or it prints as a number).
- **CI-SANDBOX-1.1 (re-freeze 4).** Verified on this daemon first: a live job container under the unfixed build carried `Binds=["/var/run/docker.sock:/var/run/docker.sock"]` (the host's rootful socket, `security=[]`) and `docker info` inside it failed `permission denied` (`.scratch/tmp/keep-q19-red-unfixed.log` — the job container vanished after the failed `docker info` on the first try, so `fixture-docker.yml` swallows the status and sleeps 25 s to stay inspectable). The fix is `--container-daemon-socket d.cfg.DockerHost` on every act invocation, `DOCKER_HOST` unchanged; the argv test pins it; `runner/README.md` names it. Q19 is in group A with the flag dropped as its mutant.
- **Harness — the negatives run in three mutant groups.** P1 applied every mutant in one build; Q5 and Q8 run on the merge gate that Q5a's mutant removes, and Q8's untrusted candidate is what Q5's mutant classes trusted, so `q-mutants.sh apply` takes row names and the battery sabotages A = `Q4 Q5a Q6 Q10 Q11 Q12 Q13 Q16 Q19` together, then B = `Q5`, then C = `Q8`. Q6 in the mutant phases stages its own untrusted candidate (a seam poke with `~put` as the actor) rather than reading Q5's env file — under the real build Q7 had already superseded that candidate, which turned the first GREEN of group A red on Q6 alone (`.scratch/tmp/p2-q-negatives-green-Q4-Q5a-Q6-Q10-Q11-Q12-Q13-Q16.log`, 7/8).
- **Harness — the P1 ghost, twice.** A re-enrolled daemon a left its old record fresh for five minutes and the scheduler handed a plan to it (Q12's first run); Q12 targets daemon b by `%assign` instead of stopping a, and Q13 waits twenty seconds after stopping a so eyre forgets the dead poll before the `%assign` (an assignment answered into it would be re-offered two minutes on, past the row's deadline).
- **Harness — JSON numbers, the refined-list trap, the store fixture.** `number-at` parses with `dim:ag` (`slaw %ud` and `dem:ag` want thousands dots: a 4389-byte size was "not a natural number"); a `(list)` refined non-empty by `?~` and handed to a wet gate fails `mull-grow`, so each call site casts back; `store.sh` runs RustFS 1.0.0 as container root (`--user 0:0`: the image's uid 10001 maps to a subuid under rootless Docker and cannot write the bind mount), the bucket is private (an unsigned list answers 403), the key is minted per fixture, and H2 points `%storage` at it so the P1 rows upload real logs (the P0 negatives' H15 expects the fixture's endpoint).
- **The cold runs.** Run 1 was stopped at P15 when re-freeze 2 landed (P0 7/7 RED and GREEN, P1 P1–P14 PASS: `.scratch/tmp/keep-cold-run1-stopped.log`); run 2 stopped at the P0 battery's foreground (`npm test` 131/132: the source test that the lift moved, fixed in `8beb59a`; `keep-cold-run2-stopped.log`); run 3 is this record, from fresh piers on the re-freeze 3 tree. Its P2 battery ran in pieces on the same ships, nothing typed between them: (i) `ci-p2` from `cold.sh`, Q2–Q9 PASS, stopped at Q10 (`keep-cold-run3-p0-p1.log`, 00:24:26) — Q10's stage poke still carried the S2 commit's seven-field shape, fixed in `affb015`; (ii) the restart refused `ci-p2` (`repository already exists`), so the battery takes `P2_REPO` and (iii) ran `ci-p2b` from Q2 through Q13 (`keep-p2-battery-attempt3-to-q13.log`, 00:34–00:44) and stopped at Q18 — the copied `p15.sh` sourced the P1 lib by a relative path, fixed in `86e3700` with `START_AT`; (iv) `START_AT=q18`: Q18, Q14–Q16, group A RED 8/8, GREEN 7/8 on Q6's stale env (`keep-p2-battery-attempt4-q18-to-negA.log`, 00:56–01:53); (v) re-freeze 4 landed meanwhile, so after `3b50b00` (Q19, Q6 self-sufficient) the battery resumed at group A with Q19: RED 9/9, GREEN 9/9, B, C, foreground (`p2-battery-c.log`, 02:01:16–02:54:08). Every Q2–Q19 row in the table is from `ci-p2b` or the run's own fresh repositories; no row was retried by hand.


### Close-out deviations (T1–T8; each cites what made it)

- **T1 — the capacity is the footer's.** `DAEMON_CAPACITY` (default 3) in `ci-p1/env.sh`; `p2-setup` enrolls daemon a at it once and `q-negatives.sh` re-enrolls at it; `q18.sh` reads the capacity daemon a's config carries and prints `Q18: SKIPPED — capacity N < 3; CI-DELIVERY-1.1: a t+0 squeeze leaves an attempt with events and no result until ~h1` (exit 0, so the rest of the battery runs) instead of restarting the daemon at 3 and back at 2. P1's own `p15-prep` keeps its 3 (P15 is P1's row; not touched).
- **T2 — every terminal is tmux, every stop is a pid.** The brief named `boot.sh`/`shutdown.sh`/`lib.sh`; the herdr calls were in `boot.sh`, `dojo.sh`, `shutdown.sh`, `rebuild.sh`, `ci-p1/nuke-revive.sh`, `ci-p1/negatives.sh` and `ci-p2/q-negatives.sh`, so all seven moved onto `ci-p0/tty.sh` (`tty_send`/`tty_keys`/`tty_wait`/`tty_read`; `capture-pane -J` for herdr's `recent-unwrapped`; exact-name targets `=NAME:`). The dojo shows a spinner after the prompt while it computes, so the idle-prompt wait holds under tmux exactly as under herdr. `ci-p0/shutdown.sh` SIGTERMs the king found by `/proc/<pid>/cmdline` (binary + pier), waits 120 s for every pier pid, SIGKILLs a survivor; the session ends with the process. `ci-p2/shutdown.sh` adds the store (the container's process pid from `docker inspect`, read in `/proc`, `docker rm -f`, waited gone) and the rootless daemon (`docker-rootless.sh stop`: the pidfile's dockerd verified against our data root, TERM, KILL if it survives, its rootlesskit pair) — the build's record left the daemon running; T2 stops it. `runner.sh`'s `pid_of` no longer exits the script under `set -e` when a daemon never started. A throwaway `~mep` boot (pier deleted after) smoke-tested the driver before the cold run: boot, a busy-dojo read, `rebuild.sh` under a mutant and its revert, `nuke-revive`'s y/N prompt, then this shutdown.
- **T3 — the store must answer.** `store.sh ready`: an unauthenticated GET of the root, 30 × 2 s, any HTTP 2xx/4xx passes (a private RustFS answers 403), curl's 000 never does; `p2-setup` exits non-zero naming the port on failure (the `| tail` after `store.sh start` had hidden its exit, which is how the operator's first attempt 401'd into a store that was not up). `cold.sh`'s store step is `start` + `ready` too.
- **T4 — the tighter tripwire per row; the premise about astra's strings.** Astra's `q-mutants.sh tripwire` strings are labels its rows print once they have observed the sabotage's effect (`Q<n> RED: <effect>`), not response bodies or refusal reasons as the brief supposed; opus's are the row's own check lines. Kept where the check line pins the sabotaged build's own answer — Q4 (200 for `../x`; astra's is the same status), Q5 (`%trusted` stored), Q8 (accepted), Q10 (the grant line), Q11 (the value's count in the object), Q12 (daemon b's sandbox line), Q19 (the host bind string). Changed where the check line pinned only a status or an absence a failure for another reason could reproduce — **Q5a** (was the merge route's 200), **Q6** (was `master unmoved …: FAIL (observed:` with no value), **Q13** (was a count of 0 refusal lines: an undelivered assignment reads the same), **Q16** (was one route's anonymous 200) — now astra's label, which the row prints only after verifying the precondition and the effect (Q5a: the ref reads CI-protected and master moved; Q6: the candidate is `%untrusted` and master is its object; Q13: the late stream shows a non-zero token length with act past the expiry; Q16: all five reads and the action answered 200 anonymously). All eleven fired in this run's RED phases, 0 wrong-reason.
- **T5 — the mount walk.** Astra's planted `p2-sandbox-inspect.sh`/`p2-sandbox-watch.py` inspect a job container's every `.Mounts` entry (for a `/tmp` alias, Rider 4); the port here is the negative the brief names: q19.sh lists every mount (type, source, destination) and every `HostConfig.Binds` entry of the live act container and asserts zero have the host socket (`/var/run/docker.sock` or its realpath `/run/docker.sock`) as source, whatever the destination. GREEN 0 of 4; RED 2 (the host bind in both views). The mutant and the first three checks are unchanged.
- **T7 — the P1 ghost, thrice: the first Q18 of this run FAILED on a stale daemon record, and the battery resumed by script.** `select-daemon` (`urgit-ci.hoon:823–835`) admits any daemon record `last-seen` within `stale-after` ~m5 (:36), fewest running first; Q12 enrolls daemon b (capacity 1, wrong key) and stops it at ~00:19:50, Q13 takes 66 s, and Q18's eight jobs were planned at 00:21:30 — 100 s later. Daemon a (capacity 3) took the plan and three jobs; the fourth, `fixtures.yml/plan` (`0v1.6vgic…`), went to b's record. `handle-assignment-poll` (:1640–1646) offers an assignment only to its own daemon and `stale-delivery` (:1597–1608) re-offers only a *delivered* one, again to that daemon; b never polled again, so the assignment was never fetched, the attempt sat `%running` with 0 events, the three gated jobs were never planned, and at the `default-deadline` ~h1 (:34, :251–263) the attempt closed `%infrastructure-error 'no result arrived before the deadline'` (01:21:30), the candidate `%unknown` `'job plan in fixtures.yml had an infrastructure error'`, **3627 s**; P15's wait saw `%unknown`, `Q18: FAIL`, `battery.sh` and `cold.sh` stopped (01:23:06), ships up. Evidence: `keep-q18-ghost-first.log`, `keep-q18-ghost-first-p15.log`, `keep-q18-ghost-first-candidate.json` (the attempt's `daemon=0v7.63ate…` = daemon b's id in `keep-q18-ghost-daemon-b.log`), `keep-cold-run1-to-q18.log`. Product side this is the CI-DELIVERY-1.1 family — an assignment bound to a daemon that stops polling is never re-homed — P1 code under the fence, P3's item; nothing in `desk/` or `runner/` was touched. Harness side, Q18 is the only P2 row whose work exceeds a's capacity, so only it can reach a ghost (P1's P13 waits 5 min 10 s for its own ghost before P15); the build's record never ran Q12 and Q18 within ~m5 of each other (its Q18 came in a later resumption). Fix (T7 commit): `q18.sh` calls `wait_ghosts_stale` (`ci-p2/lib.sh`: a dojo read of every daemon record's `last-seen`, until each but a is older than ~m5; `Q18: not run` after 400 s) before its push. Resumption: `cold.sh` gained `COLD_FROM=p2` (the P2 battery from `START_AT=<step>`, then the shutdown, appended to the same `cold.log`); `COLD_FROM=p2 START_AT=q18` at 01:24:09 on the same fresh ships, nothing typed between, ran Q18 (the wait passed at once: b's record was 65 min old), Q14–Q16, Q19, the three groups, the foreground and the shutdown. The first Q18 is recorded as FAIL; the table's Q18 row is the second run's. Observation for the operator, not a finding: the orchestrator's capacity-2 red on this branch (`battery-p2-opus-RESULT.md`) has the same signature — `fixtures.yml/plan`, the fourth of eight, 3,627 s, `%unknown` with that reason, Q12's daemon b stopped minutes before — so it may have been this ghost rather than a t+0 squeeze; its logs (`p2-opus.log`: the attempt's `daemon` id against daemon b's) would tell.
- **T7 — a prep verdict the phase counters do not carry: group B GREEN's Q2 prep, the 2 s presign.** In group B's GREEN prep (Q2 as the phase's prep, 02:21:10–02:22:43; `keep-qneg-green-Q5-prep-q2-presign-403.log`) every Q2 check passed — attempt, handle, 302, bare `curl -L` 200, sha256/size, unsigned 403, signed HEAD — except `a 2 s presign reads now: FAIL (observed: 403)`; the read after `sleep 4` answered 403 `Request has expired`. `presign-get` stamps `X-Amz-Date` = the ship's `now` in whole seconds with `X-Amz-Expires=2` (`ci-storage.hoon:115–127`), so the window is 1–2 s, and `dojo.sh` sleeps a hard 1 s before it even waits for the prompt: the driver's ≥ 1.05 s latency lost the race this once (the main table's Q2 at 00:13:50, group A's prep at 02:13:39 and the build's three instances all read 200). The 403 does not show the URL invalid before expiry; it shows the read after a 1–2 s window. The prep served its purpose (the phase got its passed attempt) and `Q5 [green]: PASS` stands on its own evidence, but the run is not failure-free: `q-negatives.sh:95` prints only `-- prep (Q2 push): Q2: FAIL` and the phase verdict counts rows. Proposed, NOT applied (the operator asked for proposals under the fence, not silent changes): sign the short presign for 5 s (the expiry read after `sleep 6`; the same two assertions, the same `Request has expired`), and have `q-negatives.sh` print a prep's failing check lines and stop the phase when the prep's essential checks (the attempt passed, a log handle) fail.
- **T7 — the other prep verdict: group A RED's Q9 prep FAILED under the mutant build, as the mutant makes it.** `keep-qneg-red-A-prep-q9.log` (01:59:32): three checks — the value in the bucket log, in the daemon's saved stream, in its set-output line — exactly the Q11 mutant's effect (the daemon's scrub skipped), which Q11's RED then trips on; the GREEN prep's Q9 passed 0-failing. Expected under `MUTATED`, but a prep line the counters hide, so it is written here.
- **T7 — what else this run reused or found.** The `~lut` pier the footer said existed did not (the operator's run 2 used `~sep`/`~pes`); the `~mep` pier did and was deleted first. The build's `.scratch/tmp` evidence was moved to `.scratch/tmp/before-closeout/` so every path this record cites is this run's; `docker-data` and the static act were kept. The P1 lib's `push_commit` prints the candidate id twice in its `-- pushed` line (`${CID:+…}${CID:-…}`), cosmetic, left alone. Astra's `p2-sandbox-inspect.sh` is a `/tmp`-alias probe (Rider 4), not a host-socket walk; T5 was built from the brief's description of it.
- **T8 — commits.** One per task above `649d9f5`, `.scratch/` added with `-f`; no rebase, squash, merge or push; both piers retained.

## Shutdown

`.scratch/ci-p2/shutdown.sh`, the last step of `cold.sh` (02:38:57–02:39:06), by /proc-verified pid throughout: the daemons (already exited with their rows), the store container by its process pid, both ships by SIGTERM to the king found through `/proc/<pid>/cmdline` (2 s each; the tmux sessions ended with the ships; no pane, no ctrl+d), then the rootless daemon and its rootlesskit pair; the piers retained; afterwards no process names either pier, `ci-p2-closeout` or `urgit-runner`, and only the operator's `ci-p2-closeout-watch` tmux session remains.

```
runner a not running
runner b not running
container urgit-ci-store-mep: running rustfs/rustfs:1.0.0 port 8365
store container urgit-ci-store-mep: pid 1793719, /proc/1793719/cmdline = /usr/bin/rustfs /data
removed urgit-ci-store-mep (data kept under /var/home/michael/workspace/urbit/urgit-ci-p2-opus/.scratch/tmp/store-data)
store pid 1793719 gone
== processes whose /proc/<pid>/cmdline is /var/home/michael/workspace/urbit/bin/urbit … /var/home/michael/piers/urgit-ci-p2-lut:
1789414  /var/home/michael/workspace/urbit/bin/urbit -F lut -B /var/home/michael/workspace/urbit/pills/brass-408k-1.pill --http-port 8359 -c /var/hom
1790292  /var/home/michael/workspace/urbit/bin/urbit work --snap-dir /var/home/michael/piers/urgit-ci-p2-lut --runtime-config 128 --temporary-cache-s
== SIGTERM to the king, pid 1789414
== all pier pids gone after SIGTERM (2026-09-18T02:39:00-05:00)
== tmux session ci-p2-closeout-lut ended with the ship
pier retained: /var/home/michael/piers/urgit-ci-p2-lut (228M)
== processes whose /proc/<pid>/cmdline is /var/home/michael/workspace/urbit/bin/urbit … /var/home/michael/piers/urgit-ci-p2-mep:
1787398  /var/home/michael/workspace/urbit/bin/urbit -F mep -B /var/home/michael/workspace/urbit/pills/brass-408k-1.pill --http-port 8358 -c /var/hom
1788316  /var/home/michael/workspace/urbit/bin/urbit work --snap-dir /var/home/michael/piers/urgit-ci-p2-mep --runtime-config 128 --temporary-cache-s
== SIGTERM to the king, pid 1787398
== all pier pids gone after SIGTERM (2026-09-18T02:39:02-05:00)
== tmux session ci-p2-closeout-mep ended with the ship
pier retained: /var/home/michael/piers/urgit-ci-p2-mep (1.1G)
dockerd pid 1793140: dockerd --config-file /var/home/michael/workspace/urbit/urgit-ci-p2-opus/.scratch/tmp/docker-config.json --data-root /va
sent TERM to 1793140
dockerd 1793140 gone; /var/home/michael/workspace/urbit/urgit-ci-p2-opus/.scratch/tmp/docker-data released
rootless daemon /run/user/1000/ci-p2-closeout down

cold.sh: every step ran (2026-09-18T02:39:06-05:00)
```
