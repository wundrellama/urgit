name: fixture-secret
on: [push]
jobs:
  secret:
    runs-on: ubuntu-latest
    steps:
      - name: use the credential
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: |
          echo "token is $TOKEN"
          echo "token length ${#TOKEN}"
          echo "leak=$TOKEN" >> "$GITHUB_OUTPUT"
