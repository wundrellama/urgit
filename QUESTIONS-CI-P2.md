# QUESTIONS — CI P2 (opus chair, branch `ci/p2-opus`)

No open boxes. The three the chair raised — §1 a non-writer's pull request needs a second ship, §2 act 0.2.89 does not mask a multi-line secret, §3 "a writer approves" needs a `can-write` read — were ratified in re-freeze 2 (`682c8cf`) and are built: the second galaxy `~put` and `.scratch/ci-p1/peer.sh` (§1), the single-line rule and the set-output scrub on both sides (§2), and `%urgit`'s `ci-can-write` peek read by `%urgit-ci` for the trust class at staging and for approval (§3). The record's Deviations name what each left behind.
